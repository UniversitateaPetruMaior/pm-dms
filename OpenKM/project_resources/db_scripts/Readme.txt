These SQL scripts may be run from the Administrator interface,
in Administration -> Database Query
( http://localhost:8080/OpenKM/admin/DatabaseQuery )
a. entering the SQL file content,
selecting JDBC Type and clicking Send button next to it
b. clicking Browse button, selecting the SQL file and clicking Execute SQL script button

* Note: be careful, in the interface, a command should be on one single row 